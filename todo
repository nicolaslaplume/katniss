Forms
   // Button size
   // FloatingAction
    Rating
    Select *
    Slider *

Surfaces
   // Accordion
   // Tabs
    Header
        Dynamic Allow injects from any place?
   // Footer

Navigation
    ContextMenu
    Pagination
    Link
        Router
        PreviewableLink (hover tooltip)
    Breadcrumbs
        the component itself is boring. a dynamic path that generates a bc that can be used easily would be better
        but that means it must be done AFTER Link and Router

Table
    sortable, pagination, sticky header

Validators
    Input Validation
    validation messages

Other
    Animation Transitions
    Typography (span / headings)
    Badge (animation)
    Chip
    Skeleton
    Speed Dial Menu

Documentation

A11Y
Event Tracking (just support view/click events)
Language Support (just a standarized method to do it)





Pro
    Date Picker (+ range)
